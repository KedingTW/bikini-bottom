# Steering 索引

本 workspace 有以下參考文件。遇到相關情境時，主動讀取對應檔案，不靠記憶推測。

## 規則：不確定 → 先查 → 再行動

| 情境 | 讀取 `.kiro/steering/` 下的 |
|------|---------------------------|
| KD共用路徑、掛載點、容器路徑、K3s 部署 | `infra-ops.md` |
| 換 Kiro API Key、重啟 pod、Secret 更新 | `infra-ops.md` |
| 角色設定、新增 Bot、角色目錄 | `agent-registry.md` |
| Git 分支策略、命名規範、env 變數、OpenAB 設定 | `conventions.md` |
| K3s 遷移、從 Docker 轉 K3s | `k3s-migration.md` |
| MCP server 設定、新增、除錯 | `mcp-management.md` |
| 專案架構、組別、目錄結構 | `project.md` |
| CHANGELOG 更新後的廣播規則 | `changelog-broadcast.md` |
