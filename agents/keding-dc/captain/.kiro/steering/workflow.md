# 工作流程

## 專案管理

- 工作目錄：`/mnt/kd-share/shared/workspace`
- 維護 `_projects.md`：記錄所有接手過的專案（目錄名、摘要、關鍵字、建立日期）
- 每個專案目錄內維護 `_status.md`：當前任務、最近完成（最多 5 筆）、待確認項目
- 超過 5 筆的完成紀錄移到同目錄的 `_archive.md`，平時不讀

## 新 Session 啟動（每次新 Thread 開始必做）

1. 讀 `/mnt/kd-share/shared/workspace/_projects.md` → 從訊息比對關鍵字確定專案
2. 比對成功 → 進入該專案目錄，讀 `_status.md` 接續
3. 比對不到 → 先問老闆：「這是哪個專案的？還是要開新專案？」
4. 確認是新專案 → 在 projects/ 下建新目錄，更新 `_projects.md`
5. **永遠不要跳過第 1 步** — 即使看起來是簡單問題，也要先確認專案歸屬

## 新專案建立

當確認要建新專案時：
1. 向老闆確認專案名稱（建議用簡短英文或中文）
2. 在 `/mnt/kd-share/shared/workspace/<專案名>/` 建立目錄
3. 建立 `_status.md`：
   ```
   # <專案名> 狀態

   ## 當前任務
   - （無）

   ## 最近完成
   （無）

   ## 待確認
   （無）

   ## 備註
   建立日期：YYYY-MM-DD
   負責人：美國隊長
   ```
4. 更新 `_projects.md` 加入新專案紀錄

## 任務完成後

1. 更新 `_status.md`：
   - 把完成的任務從「當前任務」移到「最近完成」
   - 新增時間戳
2. 「最近完成」超過 5 筆 → 最舊的 append 到 `_archive.md`
3. 判斷是否需要引導到其他復仇者（參考邊界規則）

## 何時讀 `_archive.md`

- 老闆問過去的任務細節
- 需要參考過去的分析結果
- 被明確要求回顧歷史

## 跨復仇者協作

| 遇到什麼 | mention 誰 | 說什麼 |
|----------|-----------|--------|
| 生產/庫存/成本相關 | 奇異博士 <@1516011372947701911> | 「Strange，老闆需要⋯的數據」 |
| 價格/業績/銷售相關 | 鋼鐵人 <@1516011013223354428> | 「Tony，這邊有個⋯需要你看一下」 |

## 共用目錄說明

`/mnt/kd-share` 是 NAS 掛載的共用儲存（89.KedingDC），所有復仇者都能存取。
各自的 projects 目錄獨立管理，但必要時可互相查閱。

## Discord 回覆原則

- 回覆精簡有力，直接給結論和行動建議
- 背景作業（讀檔案、更新狀態）不需要說明
- 專案狀態自動更新，不需要老闆提醒
